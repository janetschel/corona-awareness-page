import React from 'react';
import {Typography} from "@material-ui/core";

function MoreInformationWeb() {
  return(
      <div className="MoreInformationWeb">
        <Typography className="Information">Für weiterführende Informationen, öffne diese Seite auf einen nicht-mobilen Gerät</Typography>
      </div>
  );
}

export default MoreInformationWeb;
